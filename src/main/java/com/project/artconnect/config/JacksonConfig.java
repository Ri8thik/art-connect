package com.project.artconnect.config;

import org.springframework.context.annotation.Configuration;

/**
 * JSON / Jackson configuration.
 * Spring Boot 4.x auto-configures the tools.jackson.databind.ObjectMapper.
 * Add @Bean methods here for custom ObjectMapper customizations when needed.
 */
@Configuration
public class JacksonConfig {
}

